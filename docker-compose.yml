services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: marketplace-api:latest
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
