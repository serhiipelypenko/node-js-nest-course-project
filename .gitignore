node_modules/
dist/
spec.json
*.log
.idea
.DS_Store

# Секрети: у git лежать лише *.example
.env
secrets/*
!secrets/*.example

# hw-13: креденшели машини для scripts/with-secrets.sh (infisical run) —
# ключ від сховища, не сам секрет, але теж поза git.
.secrets/

# hw-15: дампи бази (scripts/backup.sh) — дані, не код
backups/

# hw-16: контракт генерується consumer-тестом (npm run test:contract) і
# в CI, і локально — не код, не потребує версіонування
pacts/
